# Default entrypoint. Shared base config + default (SUPER-based) keybinds.
# Host-specific keybind overrides live in ./hyprland/keybinds-<host>.conf,
# selected in desktop/hyprland.nix. See ./hyprland-roma.conf for an example.

source = /home/mightypancake/nixos/home/hyprland/base.conf
source = /home/mightypancake/nixos/home/hyprland/keybinds-default.conf
